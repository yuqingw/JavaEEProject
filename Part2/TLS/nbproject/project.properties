build.classes.excludes=**/*.java,**/*.form,**/.nbattrs
build.dir=build
build.generated.dir=${build.dir}/generated
client.module.uri=TLS-war
client.urlPart=/welcome.jsp
debug.classpath=${javac.classpath}::${jar.content.additional}:${run.classpath}
display.browser=true
dist.dir=dist
dist.jar=${dist.dir}/${jar.name}
j2ee.appclient.mainclass.args=-client ${dist.jar} ${j2ee.appclient.tool.args}
j2ee.deploy.on.save=true
j2ee.platform=1.5
j2ee.server.type=J2EE
jar.compress=false
jar.content.additional=\
    ${reference.TLS-war.dist-ear}:\
    ${reference.TLS-app-client.j2ee-module-car}:\
    ${reference.TLS-ejb.dist-ear}
jar.name=TLS.ear
javac.debug=true
javac.deprecation=false
javac.source=1.5
javac.target=1.5
meta.inf=src/conf
no.dependencies=false
platform.active=default_platform
project.TLS-app-client=TLS-app-client
project.TLS-ejb=TLS-ejb
project.TLS-war=TLS-war
reference.TLS-app-client.j2ee-module-car=${project.TLS-app-client}/dist/TLS-app-client.jar
reference.TLS-ejb.dist-ear=${project.TLS-ejb}/dist/TLS-ejb.jar
reference.TLS-war.dist-ear=${project.TLS-war}/dist/TLS-war.war
resource.dir=setup
run.classpath=
source.root=.
